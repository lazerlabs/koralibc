/**
 * KoraLibC errno.c - Error Number Variable Implementation
 */

/**
 * errno - Global error number variable
 *
 * This is the actual definition of the errno variable declared in errno.h.
 * It's initialized to 0 (no error) by default.
 */
int errno = 0;
